// Find and replace all product onClick handlers
// Line 456: First carousel (rows 0,1)
onClick={() => { sessionStorage.setItem('scrollPosition', window.scrollY); navigate(`/product/${product.id}`); }}

// Line 472: Filtered products carousel
onClick={() => { sessionStorage.setItem('scrollPosition', window.scrollY); navigate(`/product/${product.id}`); }}

// Line 492: Featured products
onClick={() => { sessionStorage.setItem('scrollPosition', window.scrollY); navigate(`/product/${product.id}`); }}

// Line 502: Rows 3,4 carousel
onClick={() => { sessionStorage.setItem('scrollPosition', window.scrollY); navigate(`/product/${product.id}`); }}

// Line 522: Remaining products list
onClick={() => { sessionStorage.setItem('scrollPosition', window.scrollY); navigate(`/product/${product.id}`); }}

// Line 540: Extra products grid
onClick={() => { sessionStorage.setItem('scrollPosition', window.scrollY); navigate(`/product/${product.id}`); }}
